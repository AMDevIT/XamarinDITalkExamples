<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XamarinExNoDIF"
             xmlns:viewmodels="clr-namespace:XamarinExNoDIF.ViewModels"
             x:Class="XamarinExNoDIF.MainPage">
    <ContentPage.BindingContext>
        <viewmodels:MainPageViewModel />
    </ContentPage.BindingContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Text="Xamarin Forms NO Depedendancy Framework Test" 
               VerticalOptions="Center" 
               HorizontalOptions="Center" 
               Grid.Row="0"/>
        <StackLayout Grid.Row="1" Orientation="Horizontal">
            <Label Text="Init start: " Margin="10" />
            <Label Text="{Binding InitStart}" />
        </StackLayout>

        <StackLayout Grid.Row="2" Orientation="Horizontal">
            <Label Text="Init ends: " Margin="10" />
            <Label Text="{Binding InitEnd}" />
        </StackLayout>

        <StackLayout Grid.Row="3" Orientation="Horizontal">
            <Label Text="Init ends: " Margin="10" />
            <Label Text="{Binding Duration}" />
        </StackLayout>
    </Grid>

</ContentPage>